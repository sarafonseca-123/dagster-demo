---
name: Test
  push:
    branches: ['**']


jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Test
        run: |
          echo "Hello, world!"
